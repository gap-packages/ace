
# The `hard' trivial group. We start with <r,s,t | r^s=rr,s^t=ss,t^r=tt>,
# put R=r^sr^-2 etc., then R^S=RR etc., and expand.

enum: E2;
gr: r,s,t;
rel: (r^s RR)^(s^t SS)=(r^s RR)^2, (s^t SS)^(t^r TT)=(s^t SS)^2,
     (t^r TT)^(r^s RR)=(t^r TT)^2;

wo:2000M;
mess:-10000000;
hard;

gen:;    end;
gen:r;   end;
gen:s;   end;
gen:t;   end;
gen:r,s; end;
gen:r,t; end;
gen:s,t; end;

