<!-- W  options.tex         ACE documentation - options      Alexander Hulpke / W                                                      Joachim Neub"user / W                                                            Greg Gamble / Y  Copyright (C) 2000  Centre for Discrete Mathematics and Computing / Y                      Department of Information Tech. & Electrical Eng. / Y                      University of Queensland, Australia. -->
<Chapter Label="Options for ACE">
<Heading>Options for ACE</Heading>
&ACE; offers a wide range of options to  direct  and  guide  a  coset
enumeration, most of which  are  available  from  &GAP;  through  the
interface provided by the &ACE; Package.  We  describe  most  of  the
options available via the interface in this  chapter;  other  options,
termed strategies, are defined in Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>.
(Strategies are merely special options of &ACE; that set a number  of
options described in this chapter, all at once.)  Yet  other  options,
for  which  interactive  function   alternatives   are   provided   in
Chapter&nbsp;<Ref Chap="Functions for Using ACE Interactively"/>, or which most  &GAP;
users are unlikely to  need,  are  described  in  Appendix&nbsp;<Ref Chap="Other ACE Options"/>. From within a &GAP; session, one may see the complete  list
of  &ACE;  options,   after   loading   the   &ACE;   Package   (see
Section&nbsp;<Ref Sect="Loading the ACE Package"/>), by typing
<Example><![CDATA[
gap> RecNames(KnownACEOptions);
[ "default", "help", "check", "generators", "start", "cycles", "normal", 
  "ds", "group", "subgroup", "relators", "order", "max", "rep", "system", 
  "silent", "ct", "easy", "time", "begin", "text", "options", "fill", 
  "aceinfile", "aceignore", "aceignoreunknown", "acenowarnings", "aceecho", 
  "aceincomment", "aceexampleoptions", "lenlex", "semilenlex", "incomplete", 
  "sg", "rl", "aep", "ai", "ao", "aceoutfile", "asis", "bye", "exit", "qui", 
  "cc", "cfactor", "redo", "compaction", "continu", "dmode", "dsize", "dr", 
  "dump", "echo", "enumeration", "felsch", "ffactor", "hard", "hlt", "hole", 
  "lookahead", "loop", "mendelsohn", "messages", "monitor", "mode", "nc", 
  "no", "oo", "path", "pmode", "psize", "sr", "print", "purec", "purer", 
  "rc", "recover", "contiguous", "rfactor", "rt", "row", "sc", "stabilising", 
  "sims", "standard", "statistics", "stats", "style", "tw", "trace", 
  "workspace" ]
]]></Example>
(See Section&nbsp;<Ref Sect="The KnownACEOptions Record"/>.) Also, from within a &GAP;
session, you may use &GAP;'s help browser (see Chapter&nbsp;<Ref Chap="The Help System" BookName="ref"/> in the  &GAP;  Reference  Manual);  to  find  out  about  any
particular &ACE; option, simply type: <Q>&#x2018;?option  <A>option</A></Q>',  where
<A>option</A> is one of the options listed above without any quotes, e.g.
<Log><![CDATA[
gap> ?option echo
]]></Log>
will display the section in this  manual  that  describes  the  <C>echo</C>
option.
<P/>
We begin this chapter with several sections discussing the  nature  of
the options provided. Please spend some time reading  these  sections.
To continue onto the next section on-line using &GAP;'s help browser,
type:
<Log><![CDATA[
gap> ?>
]]></Log>
<Section Label="Passing ACE Options">
<Heading>Passing ACE Options</Heading>
Options are passed to the &ACE; interface functions in either of the
two usual mechanisms provided by &GAP;, namely:
<List>
<Item> options may be set globally using the function <C>PushOptions</C>
(see Chapter&nbsp;<Ref Chap="Options Stack" BookName="ref"/> in the &GAP; Reference  Manual); or

</Item>
<Item> options may be appended to the argument list of any function
call, separated by a colon from the argument list  (see  <Ref Sect="Function Calls" BookName="ref"/> in the &GAP; Reference Manual), in which case  they  are  then
passed on recursively to any subsequent inner function call, which may
in turn have options of their own.</Item>
</List>
In general, if &ACE; is to be used  interactively  one  should  avoid
using the global method of passing options. In fact, it is recommended
that prior to calling <C>ACEStart</C> the <C>OptionsStack</C> be empty.
</Section>
<Section Label="Warnings regarding Options">
<Heading>Warnings regarding Options</Heading>
As mentioned above, one can set options globally  using  the  function
<C>PushOptions</C> (see Chapter&nbsp;<Ref Chap="Options Stack" BookName="ref"/> in the &GAP; Reference
Manual); however, options pushed onto  <C>OptionsStack</C>,  in  this  way,
remain there  until  an  explicit  <C>PopOptions()</C>  call  is  made.  In
contrast, options passed in the usual way behind a colon  following  a
function's arguments (see <Ref Sect="Function Calls" BookName="ref"/> in the &GAP; Reference
Manual)  are  local,  and  disappear  from  <C>OptionsStack</C>  after  the
function has executed successfully; nevertheless, a function, that  is
passed options this way, will also  see  any  global  options  or  any
options passed down recursively from functions calling that  function,
unless those  options  are  over-ridden  by  options  passed  via  the
function. Also note that duplication of  option  names  for  different
programs  may  lead   to   misinterpretations.   Since   a   non-empty
<C>OptionsStack</C> is potentially a mine-field for the  unwary  user,  the
function  <C>ResetOptionsStack</C>  (see&nbsp;<Ref Func="ResetOptionsStack" BookName="ref"/>   in   the
Reference Manual) is now in the &GAP; library and
<ManSection>
<Func Name="FlushOptionsStack" Arg=""/>
<Description>
introduced in version 3.001 of the &ACE; Package to perform the  same
function, is now a synonym for <C>ResetOptionsStack</C>; it simply executes
<C>PopOptions()</C> until <C>OptionsStack</C> is empty.
<P/>
However, <C>ResetOptionsStack</C> (or <C>FlushOptionsStack</C>)  does  not  wipe
out the options already passed to an <E>interactive</E> &ACE; process.  We
have provided <C>GetACEOptions</C> (see&nbsp;<Ref Func="GetACEOptions"/>) to keep  track  of
options that the &ACE; binary process still considers  active,  which
may  or  may  not  be  still  on  the  <C>OptionsStack</C>.  There  is  the
interactive  <C>SetACEOptions</C>  (see&nbsp;<Ref Func="SetACEOptions"/>)  to  change   such
options, or,  of  course,  you  can  always  elect  to  use  <C>ACEQuit</C>
(see&nbsp;<Ref Func="ACEQuit"/>) and then start a new interactive &ACE; process.
<P/>
Finally,       if       <C>ACEIgnoreUnknownDefault       :=       false</C>
(see&nbsp;<Ref Func="ACEIgnoreUnknownDefault"/>), there will  be  situations  where  an
&ACE; interface function  needs  to  be  told  explicitly  to  ignore
options passed down recursively to it from calling functions. For this
purpose  we  have  provided  the  options   <C>aceignore</C>   (see&nbsp;<Ref Subsect="option aceignore"/>) and <C>aceignoreunknown</C> (see&nbsp;<Ref Subsect="option aceignoreunknown"/>).
</Description>
</ManSection>
</Section>
<Section Label="Abbreviations and mixed case for ACE Options">
<Heading>Abbreviations and mixed case for ACE Options</Heading>
Except for limitations imposed by &GAP;  e.g.  clashes  with  &GAP;
keywords and blank spaces not allowed in keywords, the options of  the
&ACE; interface are the same as for the binary; so, for example,  the
options can appear in upper or lower case (or indeed, mixed case)  and
most may be abbreviated. Below we only list the options in  all  lower
case, and in their longest form; where  abbreviation  is  possible  we
give the shortest abbreviation in the  option's  description  e.g.&nbsp;for
the <C>mendelsohn</C> option we state that  its  shortest  abbreviation  is
<C>mend</C>, which means <C>mende</C>, <C>mendel</C> etc.,  and  indeed,  <C>Mend</C>  and
<C>MeND</C>, are all valid abbreviations of that option. Some options  have
synonyms e.g.&nbsp;<C>cfactor</C> is an alternative for <C>ct</C>.
<P/>
The complete list of &ACE; options  known  to  the  &ACE;  interface
functions, their abbreviations and the values that they are  known  to
take  may  be  gleaned  from   the   <C>KnownACEOptions</C>   record   (see
Section&nbsp;<Ref Sect="The KnownACEOptions Record"/>).
<P/>
Options  for each of  the       &ACE;       interface       functions
<C>ACECosetTableFromGensAndRels</C>,   <C>ACECosetTable</C>,   <C>ACEStats</C>    and
<C>ACEStart</C> (see  Chapter&nbsp;<Ref Chap="Functions for Using ACE Interactively"/>),
comprise the few  non-&ACE;-binary  options  (<C>silent</C>,  <C>aceinfile</C>,
<C>aceoutfile</C>,   <C>aceignore</C>,   <C>aceignoreunknown</C>,    <C>acenowarnings</C>,
<C>aceincomment</C>,     <C>aceecho</C>     and     <C>echo</C>)     discussed     in
Section <Ref Sect="Non-ACE-binary Options"/>,  (almost)  all  single-word  &ACE;
binary options and  <C>purer</C>  and  <C>purec</C>.  The  options  <C>purer</C>  and
<C>purec</C> give  the  &ACE;  binary  options  <C>pure  r</C>  and  <C>pure  c</C>,
respectively; (they are the only multiple-word &ACE;  binary  options
that do not have a single word alternative).  The  <E>only</E>  single-word
&ACE;  binary  options  that  are  <E>not</E>  available  via  the  &ACE;
interface are abbreviations that clash with &GAP; keywords (e.g.&nbsp;<K>fi</K>
for <C>fill</C>, <K>rec</K> for <C>recover</C> and  <C>continu</C>  for  <K>continue</K>).  The
detail of this paragraph is  probably  of  little  importance  to  the
&GAP; user; these comments have been included for the user who wishes
to reconcile the respective functionalities of  the  &ACE;  interface
and  the  &ACE;  standalone,  and  are  probably  of  most  value  to
standalone users.
</Section>
<Section Label="Honouring of the order in which ACE Options are passed">
<Heading>Honouring of the order in which ACE Options are passed</Heading>
<E>Note:</E> Below we describe the intended behaviour,  but  unfortunately,
since &GAP; 4.5  (approximately  when  &ACE; 5.1  was  released) the
order of options  behind  the colon  is no longer honoured. Until this
is fixed, if the order of &ACE; options needs to be respected,  users
should use &ACE; interactively (see&nbsp;<Ref Func="ACEStart"/>).
<P/>
It  is  important  to  realize  that  &ACE;'s   options   (even   the
non-strategy options) are not orthogonal, i.e.  the  order  in  which
they are put to &ACE; can be important. For this reason, except for a
few options that have no effect on the course of an  enumeration,  the
order in which options are passed to the &ACE; interface is preserved
when those same options are passed to the &ACE; binary.  One  of  the
reasons for the non-orthogonality of options is to  protect  the  user
from obtaining invalid enumerations from bad combinations of  options;
another reason is that commonly one may specify a strategy option  and
override some of that strategy's defaults; the general  rule  is  that
the later option prevails. By the way, it's not illegal to select more
than one strategy, but it's not sensible; as just mentioned, the later
one prevails.
</Section>
<Section Label="What happens if no ACE Strategy Option or if no ACE Option is passed">
<Heading>What happens if no ACE Strategy Option or if no ACE Option is passed</Heading>
If  an  &ACE;  interface  function   (<C>ACECosetTableFromGensAndRels</C>,
<C>ACEStats</C>,  <C>ACECosetTable</C>  or  <C>ACEStart</C>)  is  given  no  strategy
option, the <C>default</C>  strategy  (see  Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>) is selected, and a number of options that &ACE; needs to have a
value for are given default values, <E>prior</E> to the  execution  of  any
user options, if any. This ensures that &ACE; has a value for all its
<Q>run parameters</Q>;  three  of  these  are  defined  from  the  &ACE;
interface function arguments; and the remaining <Q>run parameters</Q>, we
denote by <Q>&ACE; Parameter Options</Q>. For user convenience, we  have
provided the <C>ACEParameterOptions</C> record (see&nbsp;<Ref Func="ACEParameterOptions"/>),
the fields of which are the <Q>&ACE; Parameter Options</Q>. The value of
each field (option) of the <C>ACEParameterOptions</C> record  is  either  a
default value or (in the case of an option that is set by a  strategy)
a record of default values that &ACE; assumes when the user does  not
define a value for  the  option  (either  indirectly  by  selecting  a
strategy option or directly).
<P/>
If the <C>default</C> strategy does not suffice, most usually a  user  will
select one of the other strategies  from  among  the  ones  listed  in
Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>, and possibly modify  some  of  the
options by selecting from  the  options  in  this  chapter.  It's  not
illegal to select more than one strategy, but it's  not  sensible;  as
mentioned above, the later one prevails.
</Section>
<Section Label="Interpretation of ACE Options">
<Heading>Interpretation of ACE Options</Heading>
Options may be given a value by an assignment to  the  name  (such  as
<C>time := <A>val</A></C>); or be passed without assigning  a  value,  in  which
case &GAP; treats the option as <E>boolean</E> and sets the option to  the
value <K>true</K>, which  is  then  interpreted  by  the  &ACE;  interface
functions. Technically speaking the &ACE; binary itself does not have
boolean options, though it does have some options which  are  declared
by passing without a value  (e.g.  the  <C>hard</C>  strategy  option)  and
others that are boolean in the C-sense (taking on just the values 0 or
1).   The   behaviour    of    the    &ACE;    interface    functions
(<C>ACECosetTableFromGensAndRels</C>,   <C>ACEStats</C>,   <C>ACECosetTable</C>    or
<C>ACEStart</C>) is essentially  to  restore  as  much  as  is  possible  a
behaviour that mimics the &ACE; standalone; a <K>false</K> value is always
translated to 0 and <K>true</K> may be translated to any of no-value, 0  or
1. Any option passed with an assigned value <A>val</A> other  than  <K>false</K>
or <K>true</K> is passed with the value <A>val</A> to the &ACE;  binary.  Since
this may appear confusing, let's consider some examples.
<List>
<Item> The <C>hard</C> strategy option  (see&nbsp;<Ref Subsect="option hard"/>)  should  be
passed without a value, which in turn is passed to the  &ACE;  binary
without  a  value.  However,  the  &ACE;  interface  function  cannot
distinguish the option <C>hard</C> being passed without a  value,  from  it
being passed via <C>hard := true</C>. Passing <C>hard := false</C> or  <C>hard  :=
<A>val</A></C> for any non-<K>true</K> <A>val</A> will however produce a warning message
(unless the option <C>acenowarnings</C> is passed) that the  value  0  (for
<K>false</K>) or <A>val</A> is unknown for that  option.  Nevertheless,  despite
the warning, in this event, the &ACE; interface function  passes  the
value to the &ACE; binary. When the &ACE; binary sees a line that it
doesn't understand it prints a warning  and  simply  ignores  it.  (So
passing <C>hard := false</C> will produce warnings, but will  have  no  ill
effects.) The reason we still pass  <E>unknown</E>  values  to  the  &ACE;
binary is that it's conceivable a future version of the &ACE;  binary
might have  several  <C>hard</C>  strategies,  in  which  case  the  &ACE;
interface function will still complain (until it's made aware  of  the
new possible values) but it will perform in the correct  manner  if  a
value expected by the &ACE; binary is passed.

</Item>
<Item> The <C>felsch</C> strategy option (see&nbsp;<Ref Subsect="option felsch"/>)  may  be
passed without a value (which chooses the <E>felsch 0</E> strategy) or with
the values 0 or 1. Despite the fact that &GAP; sees  this  option  as
<E>boolean</E>; it is <E>not</E>. There are two Felsch  strategies:  <E>felsch  0</E>
and <E>felsch 1</E>. To get the <E>felsch 1</E> strategy,  the  user  must  pass
<C>felsch := 1</C>. If the user were to pass <C>felsch := false</C>  the  result
would be the <E>felsch 0</E> strategy (since <K>false</K> is  always  translated
to 0), i.e.&nbsp;the same as how <C>felsch := true</C> would be interpreted.  We
could protect the user more from  such  ideosyncrasies,  but  we  have
erred on the side of simplicity in order to make  the  interface  less
vulnerable to upgrades of the &ACE; binary.</Item>
</List>
The lesson from the two examples is: <E>check the documentation  for  an
option to see  how  it  will  be  interpreted</E>.  In  general,  options
documented (in this chapter) as <E>only</E> being no-value options  can  be
safely thought of as boolean (i.e.&nbsp;you will get  what  you  expect  by
assigning <K>true</K> or  <K>false</K>),  whereas  strategy  (no-value)  options
should <E>not</E> be thought of as boolean (a <K>false</K> assignment will <E>not</E>
give you what you might have expected).
<P/>
Options that are unknown to the &ACE;  interface  functions  and  not
ignored (see below), that are passed without  a  value,  are  <E>always</E>
passed to the &ACE; binary  as  no-value  options  (except  when  the
options are ignored); the user can over-ride this behaviour simply  by
assigning the intended value. Note that it is perfectly safe to  allow
the &ACE; binary to be passed unknown options,  since  &ACE;  simply
ignores options it doesn't understand, issues an error message  (which
is just a warning and is output by &GAP; unless <C>acenowarnings</C>  (see
<Ref Subsect="option acenowarnings"/>) is passed) and continues  on  with  any  other
options passed in exactly the way it would  have  if  the  <Q>unknown</Q>
options had not been passed.
<P/>
An option is  ignored  if  it  is  unknown  to  the  &ACE;  interface
functions and one of the following is true:
<List>
<Item> the global  variable  <C>ACEIgnoreUnknownDefault</C>  is  set  to
<K>false</K> (see&nbsp;<Ref Func="ACEIgnoreUnknownDefault"/>) or,

</Item>
<Item> the <C>aceignoreunknown</C> option (see&nbsp;<Ref Subsect="option aceignoreunknown"/>)
is passed, or 

</Item>
<Item> the <C>aceignore</C> option  is  passed  and  the  option  is  an
element of the list value of <C>aceignore</C> (see&nbsp;<Ref Subsect="option aceignore"/>).</Item>
</List>
<Index>debugging</Index>
<Index>ACEIgnoreUnknownDefault!use as debugging tool</Index>
It    is    actually    <E>recommended</E>    that     the     user     set
<C>ACEIgnoreUnknownDefault</C> to <K>false</K>, since this will allow  the  user
to see when  &ACE;  functions  have  been  passed  options  that  are
<Q>unknown</Q> to the &ACE; package.  In  this  way  the  user  will  be
informed about misspelt options, for example. So it's a good debugging
tool. Also, if the &ACE; binary is updated with a  version  with  new
options then these will not be known by the package (the &GAP;  part)
and it will be necessary to set <C>ACEIgnoreUnknownDefault</C>  to  <K>false</K>
in order for the new options to be  passed  to  the  binary.  When  an
&ACE; function is invoked indirectly by some function that was called
with non-&ACE; options the warning messages may begin to be annoying,
and it's then a simple matter to set <C>ACEIgnoreUnknownDefault</C> back to
the &ACE; 3.003 default value of <K>true</K>.
<P/>
Warning messages regarding unknown  options  are  printed  unless  the
<C>acenowarnings</C> (see <Ref Subsect="option acenowarnings"/>) is passed or  the  option
is ignored.
<P/>
To see how options are interpreted by an  &ACE;  interface  function,
pass the <C>echo</C> option.
<P/>
As  mentioned  above,  any  option  that  the  &ACE;  binary  doesn't
understand is simply ignored and a warning appears in the output  from
&ACE;. If this occurs, you may wish to check the input fed to  &ACE;
and the output from &ACE;, which when &ACE; is run non-interactively
are stored in files whose full path names are recorded in  the  record
fields   <C>ACEData.infile</C>   and    <C>ACEData.outfile</C>,    respectively.
Alternatively, both interactively and non-interactively  one  can  set
the <C>InfoLevel</C> of <C>InfoACE</C> to 3 (see&nbsp;<Ref Func="InfoACE"/>), to see  the
output from &ACE;, or to 4 to  also  see  the  commands  directed  to
&ACE;.
</Section>
<Section Label="An Example of passing Options">
<Heading>An Example of passing Options</Heading>
Continuing with the example of Section&nbsp;<Ref Sect="Using ACE Directly to Generate a Coset Table"/>, one could set the <C>echo</C> option to be  true,  use  the
<C>hard</C> strategy option, increase the workspace  to  <M>10^7</M>  words  and
turn messaging on (but to be fairly infrequent) by setting  <C>messages</C>
to a large positive value as follows:
<Log><![CDATA[
gap> ACECosetTable(fgens, rels, [c]
>                  : echo, hard, Wo := 10^7, mess := 10000);;
]]></Log>
As mentioned in the previous section, <C>echo</C> may be thought  of  as  a
boolean option, whereas <C>hard</C> is a strategy option (and hence  should
be thought of as a no-value option). Also, observe  that  two  options
have  been  abbreviated:  <C>Wo</C>  is  a  mixed  case   abbreviation   of
<C>workspace</C>, and <C>mess</C> is an abbreviation of <C>messages</C>.
</Section>
<Section Label="The KnownACEOptions Record">
<Heading>The KnownACEOptions Record</Heading>
<ManSection>
<Var Name="KnownACEOptions"/>
<Description>
is a &GAP; record whose fields are the &ACE; options  known  to  the
&ACE; interface; each field (known &ACE; option) is  assigned  to  a
list of the form <C>[<A>i</A>, <A>ListOrFunction</A>]</C>, where <C><A>i</A></C> is an  integer
representing the length of the shortest abbreviation of the option and
<C><A>ListOrFunction</A></C> is either a list of (known)  allowed  values  or  a
boolean function that may be used to determine if the given value is a
(known) valid value e.g.
<Example><![CDATA[
gap> KnownACEOptions.compaction;
[ 3, [ 0 .. 100 ] ]
]]></Example>
indicates that the option <C>compaction</C> may be  abbreviated  to  <C>com</C>
and the (known) valid values are in the (integer) range 0 to 100; and
<Example><![CDATA[
gap> KnownACEOptions.ct;
[ 2, <Category "IsInt"> ]
]]></Example>
indicates that there is essentially no abbreviation of <C>ct</C> (since its
shortest abbreviation is of length 2),  and a value of  <C>ct</C> is  known
to be valid if <C>IsInt</C> returns true for that value.
<P/>
For user convenience, we provide the function
</Description>
</ManSection>
<ManSection>
<Func Name="ACEOptionData" Arg="optname"/>
<Description>
which for a string <A>optname</A> representing an &ACE; option (or a guess
of one) returns a record with the following fields:
<List>
<Mark><C>name</C></Mark>
<Item><A>optname</A> (unchanged);</Item>
<Mark><C>known</C></Mark>
<Item><K>true</K> if <A>optname</A> is a valid mixed case abbreviation
of a known &ACE; option, and false otherwise;</Item>
<Mark><C>fullname</C></Mark>
<Item>the lower case unabbreviated form of <A>optname</A> if the
<C>known</C>  field  is  set  <K>true</K>,  or  <A>optname</A>  in  all  lower  case,
otherwise;</Item>
<Mark><C>synonyms</C></Mark>
<Item>a  list  of  known  &ACE;  options  synonymous  with
<A>optname</A>, in lowercase unabbreviated form, if the  <C>known</C>  field  is
set <K>true</K>, or a list containing just <A>optname</A>  in  all  lower  case,
otherwise;</Item>
<Mark><C>abbrev</C></Mark>
<Item>the shortest lowercase abbreviation  of  <A>optname</A>  if
the <C>known</C> field is set <K>true</K>,  or  <A>optname</A>  in  all  lower  case,
otherwise.</Item>
</List>
For more on synonyms of &ACE; options, see&nbsp;<Ref Func="ACEOptionSynonyms"/>.
<P/>
The function <C>ACEOptionData</C> provides the  user  with  all  the  query
facility she should ever need; nevertheless, we provide the  following
functions.
</Description>
</ManSection>
<ManSection>
<Func Name="IsKnownACEOption" Arg="optname"/>
<Description>
returns <K>true</K> if <A>optname</A> is a mixed case abbreviation of a field of
<C>KnownACEOptions</C>,           or           <K>false</K>           otherwise.
<C>IsKnownACEOption(<A>optname</A>);</C> is equivalent to
<Log><![CDATA[
  {ACEOptionData(<optname>).known;}
]]></Log>
</Description>
</ManSection>
<ManSection>
<Func Name="ACEPreferredOptionName" Arg="optname"/>
<Description>
returns the lowercase unabbreviated first alternative of <A>optname</A>  if
it is a known &ACE; option, or  <A>optname</A>  in  lowercase,  otherwise.
<C>ACEPreferredOptionName(<A>optname</A>);</C> is equivalent to
<Log><![CDATA[
  {ACEOptionData(<optname>).synonyms[1];}
]]></Log>
</Description>
</ManSection>
<ManSection>
<Func Name="IsACEParameterOption" Arg="optname"/>
<Description>
returns true if <A>optname</A> is an <Q>&ACE; parameter  option</Q>.  (&ACE;
Parameter Options  are  described  in  Section&nbsp;<Ref Func="ACEParameterOptions"/>).
<C>IsACEParameterOption(<A>optname</A>);</C> is equivalent to
<Log><![CDATA[
  {ACEPreferredOptionName(<optname>) in RecNames(ACEParameterOptions);}
]]></Log>
</Description>
</ManSection>
<ManSection>
<Func Name="IsACEStrategyOption" Arg="optname"/>
<Description>
returns true if <A>optname</A> is  an  <Q>&ACE;  strategy  option</Q>  (see
Section&nbsp;<Ref Sect="The ACEStrategyOptions List"/>).
<C>IsACEStrategyOption(<A>optname</A>);</C> is equivalent to
<Log><![CDATA[
  {ACEPreferredOptionName(<optname>) in ACEStrategyOptions;}
]]></Log>
</Description>
</ManSection>
</Section>
<Section Label="The ACEStrategyOptions List">
<Heading>The ACEStrategyOptions List</Heading>
<ManSection>
<Var Name="ACEStrategyOptions"/>
<Description>
is a &GAP; list that contains  the  strategy  options  known  to  the
&ACE; interface functions:
<Example><![CDATA[
gap> ACEStrategyOptions;
[ "default", "easy", "felsch", "hard", "hlt", "purec", "purer", "sims" ]
]]></Example>
See Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>  for  details  regarding  the
&ACE; strategy options.
</Description>
</ManSection>
</Section>
<Section Label="ACE Option Synonyms">
<Heading>ACE Option Synonyms</Heading>
<ManSection>
<Var Name="ACEOptionSynonyms"/>
<Description>
is a &GAP; record. A number of known &ACE;  options  have  synonyms.
The fields of the <C>ACEOptionSynonyms</C>  record  are  the  <Q>preferred</Q>
option names and the values assigned to the fields are  the  lists  of
synonyms  of  those  option  names.  What   makes   an   option   name
<Q>preferred</Q> is somewhat arbitrary (in most cases, it is  simply  the
shortest of a list of  synonyms).  For  a  <Q>preferred</Q>  option  name
<A>optname</A> that has synonyms, the complete  list  of  synonyms  may  be
obtained     by     concatenating     <C>[     <A>optname</A>     ]</C>      and
<C>ACEOptionSynonyms.(<A>optname</A>)</C>, e.g.
<Example><![CDATA[
gap> Concatenation( [ "messages" ], ACEOptionSynonyms.("messages") );
[ "messages", "monitor" ]
]]></Example>
More generally, for an arbitrary option name  <A>optname</A>  its  list  of
synonyms (which may be a list of one element) may be obtained  as  the
<C>synonyms</C> field of the record returned by  <C>ACEOptionData(<A>optname</A>)</C>
(see&nbsp;<Ref Func="ACEOptionData"/>).
</Description>
</ManSection>
</Section>
<Section Label="Non-ACE-binary Options">
<Heading>Non-ACE-binary Options</Heading>
<ManSection>
<Var Name="NonACEbinOptions"/>
<Description>
is a &GAP; list of options that have  meaning  only  for  the  &ACE;
Package interface, i.e.&nbsp;options in <C>KnownACEOptions</C>  that  are  <E>not</E>
&ACE; binary options; each such option is described in detail  below.
<E>Except</E> for  the  options  listed  in  <C>NonACEbinOptions</C>  and  those
options that are excluded via the <C>aceignore</C>  and  <C>aceignoreunknown</C>
options  (described  below),   <E>all</E>   options   that   are   on   the
<C>OptionsStack</C> when an &ACE; interface function is called, are passed
to the &ACE; binary. Even options that produce the  warning  message:
<Q>&#x2018;unknown (maybe new) or bad</Q>', by virtue of not being  a  field  of
<C>KnownACEOptions</C>, are passed to the &ACE; binary  (except  that  the
options <C>purer</C> and <C>purec</C> are first translated to <C>pure r</C> and <C>pure
c</C>, respectively). When the &ACE; binary encounters an option that it
doesn't understand it issues a  warning  and  simply  ignores  it;  so
options accidentally passed to &ACE; are unlikely to pose problems.
<P/>
We also mention here, since  it  is  related  to  an  option  of  this
section, the following.
</Description>
</ManSection>
<ManSection>
<Var Name="ACEIgnoreUnknownDefault"/>
<Description>
is a global variable (<E>not</E> an option) that is initially  set  by  the
&ACE; package to <K>true</K>, and is the default action that &ACE;  takes
for options that are unknown to the &ACE; package  (but  may  be  new
options provided in a new version of the &ACE; binary).  Despite  the
fact that it is normally set <K>true</K>, it is recommended (especially for
the novice user of the &ACE; package) to set <C>ACEIgnoreUnknownDefault
:= false</C>; the worst that can happen is being annoyed by  a  profusion
of warnings of unknown options. For individual functions, the user may
use the option <C>aceignoreunknown</C> (see&nbsp;<Ref Subsect="option aceignoreunknown"/>)  to
over-ride the setting of <C>ACEIgnoreUnknownDefault</C>.
<P/>
Here now, are the  few  options  that  are  available  to  the  &GAP;
interface to &ACE; that have no counterpart in the &ACE; standalone:
</Description>
</ManSection>
<Subsection Label="option silent">
<Heading><C>silent</C></Heading>
<Index>option <C>silent</C></Index>Inhibits an <C>Error</C> return when generating a coset table.
<P/>
If a coset  enumeration  that  invokes  <C>ACECosetTableFromGensAndRels</C>
does not finish within the preset limits, an error is  raised  by  the
interface to  &GAP;,  unless  the  option  <C>silent</C>  or  <C>incomplete</C>
(see&nbsp;<Ref Subsect="option incomplete"/>) has been set; in the former case, <K>fail</K>  is
returned.  This  option  is  included  to  make   the   behaviour   of
<C>ACECosetTableFromGensAndRels</C> compatible with that  of  the  function
<C>CosetTableFromGensAndRels</C> it replaces. If the option <C>incomplete</C> is
also set, it overrides option <C>silent</C>.
</Subsection>
<Subsection Label="option lenlex">
<Heading><C>lenlex</C></Heading>
<Index>option <C>lenlex</C></Index>Ensures that <C>ACECosetTable</C> and <C>ACECosetTableFromGensAndRels</C> output
a coset table that is <C>lenlex</C> standardised.
<P/>
The  <C>lenlex</C>  scheme,  numbers  cosets  in  such  a  way  that  their
<Q>preferred</Q> (coset) representatives, in an  alphabet  consisting  of
the user-submitted generators and their inverses,  are  ordered  first
according to <C>length</C> and then according to a <C>lexical</C>  ordering.  In
order to describe what the <C>lenlex</C> scheme's  <C>lexical</C>  ordering  is,
let us consider an example. Suppose the generators  submitted  by  the
user are, in user-supplied order, <C>[x, y, a, b]</C>,  and  represent  the
inverses of these generators by the corresponding  uppercase  letters:
<C>[X, Y, A, B]</C>, then  the  <C>lexical</C>  ordering  of  <C>lenlex</C>  is  that
derived from defining <C>x &lt; X &lt; y &lt; Y &lt; a &lt; A &lt; b &lt; B</C>.
<P/>
<E>Notes:</E>
In  some  circumstances,  &ACE;  prefers  to  swap  the   first   two
generators;   such   cases    are    detected    by    the    function
<C>IsACEGeneratorsInPreferredOrder</C>                                 (see
<Ref Func="IsACEGeneratorsInPreferredOrder"/>). In such cases, special  action  is
taken to avoid &ACE; swapping the first two generators;  this  action
is   described   in   the   notes   for    <C>ACEStandardCosetNumbering</C>
(see&nbsp;<Ref Func="ACEStandardCosetNumbering"/>).  When  this   special   action   is
invoked, a side-effect is that any setting of the <C>asis</C>  (see&nbsp;<Ref Subsect="option asis"/>) option by the user is ignored.
<P/>
The  <C>lenlex</C>  standardisation  scheme  is  the  default  coset  table
standardisation  scheme  of  &GAP;. Alternatively, <C>semilenlex</C> can
be used. Both schemes are described in detail in  Section&nbsp;<Ref Sect="Coset Table Standardisation Schemes"/>.
</Subsection>
<Subsection Label="option semilenlex">
<Heading><C>semilenlex</C></Heading>
<Index>option <C>semilenlex</C></Index>Ensures that <C>ACECosetTable</C> and <C>ACECosetTableFromGensAndRels</C> output
a coset table that is <C>semilenlex</C> standardised.
<P/>
The <C>semilenlex</C> scheme, numbers cosets  in  such  a  way  that  their
<Q>preferred</Q> (coset) representatives, in an  alphabet  consisting  of
only the user-submitted generators, are  ordered  first  according  to
<C>length</C> and then according to a <C>lexical</C> ordering.
</Subsection>
<Subsection Label="option incomplete">
<Heading><C>incomplete</C></Heading>
<Index>option <C>incomplete</C></Index>Allows the return  of  an  <C>incomplete</C>  coset  table,  when  a  coset
enumeration does not finish within preset limits.
<P/>
If a coset enumeration that invokes <C>ACECosetTableFromGensAndRels</C>  or
<C>ACECosetTable</C> does not finish within the preset limits, an error  is
raised  by  the  interface  to  &GAP;,  unless  the  option  <C>silent</C>
(see&nbsp;<Ref Subsect="option silent"/>) or <C>incomplete</C> has  been  set;  in  the  latter
case, a partial coset table, that is a valid &GAP; list of lists,  is
returned. Each position of the table  without  a  valid  coset  number
entry is filled with a zero. If  the  option  <C>silent</C>  is  also  set,
<C>incomplete</C>  prevails.  An  incomplete  table  is
returned reduced (i.e.&nbsp;with  insignificant  coset  numbers  ---  those
appearing only in their place of definition --- removed) and  <C>lenlex</C>
standardised (regardless of whether  the  <C>semilenlex</C>  option  is  in
force). When an incomplete table is  returned,  a
warning is emitted at <C>InfoACE</C> or <C>InfoWarning</C> level 1.
</Subsection>
<Subsection Label="option aceinfile">
<Heading><C>aceinfile</C></Heading>
<Index>option <C>aceinfile</C></Index>Creates an &ACE; input file <A>filename</A> for use  with  the  standalone
only; <A>filename</A> should be a string. (Shortest abbreviation: <C>acein</C>.)
<P/>
This option is only relevant to <C>ACECosetTableFromGensAndRels</C> and  is
ignored if included as an option for  invocations  of  <C>ACEStats</C>  and
<C>ACEStart</C>. If this option is used, &GAP; creates an input file  with
filename <A>filename</A> only, and then exits (i.e.&nbsp;the  &ACE;  binary  is
not called). This option is  provided  for  users  who  wish  to  work
directly with the &ACE; standalone. The full path to the  input  file
normally used by &ACE; (i.e.&nbsp;when option <C>aceinfile</C> is not used)  is
stored in <C>ACEData.infile</C>.
</Subsection>
<Subsection Label="option aceoutfile">
<Heading><C>aceoutfile</C></Heading>
<Index>option <C>aceoutfile</C></Index>Redirects &ACE; output to file <A>filename</A>;  <A>filename</A>  should  be  a
string. (Shortest abbreviation: <C>aceo</C>.)
<P/>
This  is  actually  a  synonym  for  the  <C>ao</C>  option.  Please  refer
to&nbsp;<Ref Subsect="option ao"/>, for further discussion of this option.
</Subsection>
<Subsection Label="option aceignore">
<Heading><C>aceignore</C></Heading>
<Index>option <C>aceignore</C></Index>Directs an &ACE; function to  ignore  the  options  in  <A>optionList</A>;
<A>optionList</A> should be a list of strings.
(Shortest abbreviation: <C>aceig</C>.)
<P/>
If a function called with its own options, in  turn  calls  an  &ACE;
function for which those options are not intended, the &ACE; function
will pass those options to the &ACE; binary.  If  those  options  are
unknown to  the  &ACE;  interface  (and  <C>ACEIgnoreUnknownDefault  :=
false</C>      and      <C>aceignoreunknown</C>      is      not       passed;
see&nbsp;<Ref Func="ACEIgnoreUnknownDefault"/> and&nbsp;<Ref Subsect="option aceignoreunknown"/>) a warning
is issued. Options that are unknown to the &ACE;  binary  are  simply
ignored by &ACE; (and a warning that the option was  ignored  appears
in the  &ACE;  output,  which  the  user  will  not  see  unless  the
<C>InfoLevel</C> of <C>InfoACE</C> or <C>InfoWarning</C> is set to  1).  This  option
enables the user to avoid such  options  being  passed  at  all,  thus
avoiding the warning messages and also any options that coincidentally
are &ACE; options but are not intended for the &ACE; function  being
called.
</Subsection>
<Subsection Label="option aceignoreunknown">
<Heading><C>aceignoreunknown</C></Heading>
<Index>option <C>aceignoreunknown</C></Index>Directs an &ACE; function to ignore any  options  not  known  to  the
&ACE; interface.
(Shortest abbreviation: <C>aceignoreu</C>.)
<P/>
This option is provided for similar reasons to <C>aceignore</C>.  Normally,
it is safe to include it, to avoid aberrant warning messages from  the
&ACE; interface. However, fairly obviously, it should not  be  passed
without a value (or set to <K>true</K>) in the situation where a new &ACE;
binary has been installed with new options that are not  listed  among
the fields of <C>KnownACEOptions</C>, which you intend to use. Omitting the
<C>aceignoreunknown</C> option is equivalent to setting it to the value  of
<C>ACEIgnoreUnknownDefault</C> (see <Ref Func="ACEIgnoreUnknownDefault"/>); i.e.&nbsp;it  is
superfluous    if    <C>ACEIgnoreUnknownDefault    :=    true</C>    unless
<C>aceignoreunknown</C> is set to <K>false</K>.
</Subsection>
<Subsection Label="option acenowarnings">
<Heading><C>acenowarnings</C></Heading>
<Index>option <C>acenowarnings</C></Index>Inhibits the warning message <Q>&#x2018;unknown (maybe new) or bad option</Q>'
for options not listed in <C>KnownACEOptions</C>.
(Shortest abbreviation: <C>acenow</C>.)
<P/>
This option suppresses the warning messages for  unknown  options  (to
the &ACE; interface), but unlike <C>aceignore</C>  and  <C>aceignoreunknown</C>
still allows them to be passed to the &ACE; binary.
</Subsection>
<Subsection Label="option echo">
<Heading><C>echo</C></Heading>
<Index>option <C>echo</C></Index>Echoes arguments and options (and indicates how options were handled).
<P/>
Unlike the previous options of this  section,  there  <E>is</E>  an  &ACE;
binary option <C>echo</C>. However, the <C>echo</C> option  is  handled  by  the
&ACE; interface and is not passed to the &ACE; binary. (If you  wish
to put  <C>echo</C>  in  a  standalone  script  use  the  <C>aceecho</C>  option
following.) If <C>echo</C> is passed with the value 2 then a  list  of  the
options (together with their values) that are set via &ACE;  defaults
are also echoed to the screen.
</Subsection>
<Subsection Label="option aceecho">
<Heading><C>aceecho</C></Heading>
<Index>option <C>aceecho</C></Index>The &ACE; binary's <C>echo</C> command.
<P/>
This option is only included so  that  a  user  <E>can</E>  put  an  <C>echo</C>
statement in  an  &ACE;  standalone  script.  Otherwise,  use  <C>echo</C>
(above).
</Subsection>
<Subsection Label="option aceincomment">
<Heading><C>aceincomment</C></Heading>
<Index>option <C>aceincomment</C></Index>Print comment <A>string</A> in the &ACE; input; <A>string</A> must be a string.
(Shortest abbreviation: <C>aceinc</C>.)
<P/>
This option prints the comment <A>string</A> behind a sharp sign (<C>#</C>)  in
the input to &ACE;. Only useful  for  adding  comments  (that  &ACE;
ignores) to standalone input files.
</Subsection>
<Subsection Label="option aceexampleoptions">
<Heading><C>aceexampleoptions</C></Heading>
<Index>option <C>aceexampleoptions</C></Index>An <E>internal</E> option for <C>ACEExample</C>.
<P/>
This option is passed  <E>internally</E>  by  <C>ACEExample</C>  to  the  &ACE;
interface function  it  calls,  when  one  invokes  <C>ACEExample</C>  with
options. Its purpose is to provide a mechanism for the over-riding  of
an example's options by the user. The option name is deliberately long
and has no abbreviation to discourage user use.
</Subsection>
</Section>
<Section Label="ACE Parameter Options">
<Heading>ACE Parameter Options</Heading>
<ManSection>
<Var Name="ACEParameterOptions"/>
<Description>
is a &GAP; record, whose fields are the <Q>&ACE; Parameter Options</Q>.
The <Q>&ACE; Parameter Options</Q> are options which, if not supplied  a
value by the user, are supplied a default value by  &ACE;.  In  fact,
the <Q>&ACE; Parameter Options</Q> are those options that appear  (along
with <C>Group Generators</C>, <C>Group Relators</C> and  <C>Subgroup  Generators</C>,
which are defined from &ACE; interface  function  arguments)  in  the
<Q>Run Parameters</Q> block of &ACE;  output,  when,  for  example,  the
<C>messages</C> option is non-zero.
<P/>
For each field (&ACE; parameter option) of the  <C>ACEParameterOptions</C>
record, the value assigned is  the  default  value  (or  a  record  of
default values) that are supplied by &ACE; when  the  option  is  not
given a value by the user (either indirectly by selecting  a  strategy
option or directly).
<P/>
In the cases where the value of a field of  the  <C>ACEParameterOptions</C>
record is itself a record, the fields of that record are <C>default</C> and
strategies for which the value assigned by that strategy differs  from
the <C>default</C> strategy. A <Q>strategy</Q>, here, is the  strategy  option
itself, if it is only  a  no-value  option,  or  the  strategy  option
concatenated with any of its integer values  (as  strings),  otherwise
(e.g.&nbsp;<C>felsch0</C> and <C>sims9</C>  are  strategies,  and  <C>hlt</C>  is  both  a
strategy and a strategy option). As an exercise, the reader might like
to try to reproduce the table at the  beginning  of  Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/> using the <C>ACEParameterOptions</C>  record.  (Hint:  you
first need to select those fields of the <C>ACEParameterOptions</C>  record
whose values are records with at least two fields.)
<P/>
<E>Note:</E>
Where  an  <Q>&ACE;  Parameter  Option</Q>  has   synonyms,   only   the
<Q>preferred</Q> option name (see&nbsp;<Ref Func="ACEOptionSynonyms"/>) appears as a field
of <C>ACEParameterOptions</C>. The  complete  list  of  <Q>&ACE;  Parameter
Options</Q> may be obtained by
<Example><![CDATA[
gap> Concatenation( List(RecNames(ACEParameterOptions),
>                        optname -> ACEOptionData(optname).synonyms) );
[ "subgroup", "max", "ct", "cfactor", "time", "fill", "ffactor", "asis", 
  "compaction", "dmode", "dsize", "enumeration", "hole", "lookahead", "loop", 
  "mendelsohn", "messages", "monitor", "no", "path", "pmode", "psize", "rt", 
  "rfactor", "row", "workspace" ]
]]></Example>
We describe the <Q>&ACE; Parameter Options</Q> in the  Sections&nbsp;<Ref Sect="General ACE Parameter Options that Modify the Enumeration Process"/>,  <Ref Sect="ACE Parameter Options Modifying C Style Definitions"/>,  <Ref Sect="ACE Parameter Options for R Style Definitions"/>, <Ref Sect="ACE Parameter Options for Deduction Handling"/>, <Ref Sect="Technical ACE Parameter Options"/>, <Ref Sect="ACE Parameter Options controlling ACE Output"/>, and&nbsp;<Ref Sect="ACE Parameter Options that give Names to the Group and Subgroup"/>, following.
</Description>
</ManSection>
</Section>
<Section Label="General ACE Parameter Options that Modify the Enumeration Process">
<Heading>General ACE Parameter Options that Modify the Enumeration Process</Heading>
<Subsection Label="option asis">
<Heading><C>asis</C></Heading>
<Index>option <C>asis</C></Index>Do not reduce relators. (Shortest abbreviation: <C>as</C>.)
<P/>
By default, &ACE; freely  and cyclically reduces the relators, freely
reduces  the  subgroup generators,  and  sorts  relators and  subgroup
generators in length-increasing  order.  If you do not  want this, you
can switch it off by setting the <C>asis</C> option.
<P/>
<E>Notes:</E> As well as allowing you  to use the presentation <E>as</E> it <E>is</E>
given,  this  is  useful for  forcing  definitions  to  be made  in  a
prespecified  order,  by  introducing  dummy  (i.e.,  freely  trivial)
subgroup generators.   (Note that the  exact form of  the presentation
can  have a significant  impact on  the enumeration  statistics.)  For
some fine points of the influence of <C>asis</C> being set on the treatment
of involutory generators see the &ACE; standalone manual.
</Subsection>
<Subsection Label="option ct">
<Heading><C>ct</C>, <C>cfactor</C></Heading>
<Index>option <C>ct</C></Index><Index>option <C>cfactor</C></Index>Number of C style definitions per pass; <C><A>val</A></C> should be an  integer. 
(Shortest abbreviation of <C>cfactor</C> is <C>c</C>.)
<P/>
The absolute value of <C><A>val</A></C> sets the number of C  style  definitions
per pass through the enumerator's main loop. The sign of <C><A>val</A></C>  sets
the style. The possible combinations of the values of  <C>ct</C>  and  <C>rt</C>
(described below) are given in the  table  of  enumeration  styles  in
Section&nbsp;<Ref Sect="Enumeration Style"/>.
</Subsection>
<Subsection Label="option rt">
<Heading><C>rt</C>, <C>rfactor</C></Heading>
<Index>option <C>rt</C></Index><Index>option <C>rfactor</C></Index>Number of R style definitions per pass; <C><A>val</A></C> should be an  integer. 
(Shortest abbreviation of <C>rfactor</C> is <C>r</C>.)
<P/>
The absolute value of <C><A>val</A></C> sets the number of R  style  definitions
per pass through the enumerator's main loop. The sign of <C><A>val</A></C>  sets
the style. The possible combinations of the values of <C>ct</C>  (described
above) and <C>rt</C> are given  in  the  table  of  enumeration  styles  in
Section&nbsp;<Ref Sect="Enumeration Style"/>.
</Subsection>
<Subsection Label="option no">
<Heading><C>no</C></Heading>
<Index>option <C>no</C></Index>The number of group relators to include in the subgroup;  
<C><A>val</A></C> should be an integer greater than or equal to <M>-1</M>.
<P/>
It is sometimes helpful to include the group relators into the list of
the subgroup generators, in the sense that they are applied  to  coset
number 1 at the start of an enumeration. A value of 0 for this  option
turns this feature off and the (default) argument of <M>-1</M> includes all
the relators. A positive argument includes  the  specified  number  of
relators,  in  order.  The  <C>no</C>  option  affects  only  the   C style
procedures.
</Subsection>
<Subsection Label="option mendelsohn">
<Heading><C>mendelsohn</C></Heading>
<Index>option <C>mendelsohn</C></Index>Turns on mendelsohn processing. (Shortest abbreviation: <C>mend</C>.)
<P/>
Mendelsohn style processing during relator scanning/closing is  turned
on by giving this option. Off is the default, and  here  relators  are
scanned only from the start (and end) of a relator. Mendelsohn  <Q>on</Q>
means that all  (different)  cyclic  permutations  of  a  relator  are
scanned.
<P/>
The effect of Mendelsohn style processing  is  case-specific.  It  can
mean the difference between success or failure, or it can  impact  the
number of coset numbers required, or it  can  have  no  effect  on  an
enumeration's statistics.
<P/>
<E>Note:</E> Processing all cyclic permutations of the relators can be very
time-consuming,  especially if  the  presentation is  large.  So,  all
other things being equal, the  Mendelsohn flag should normally be left
off.
</Subsection>
</Section>
<Section Label="ACE Parameter Options Modifying C Style Definitions">
<Heading>ACE Parameter Options Modifying C Style Definitions</Heading>
<Index>C style<Subkey>definition</Subkey></Index>
The  next  three  options  are  relevant  only  for  making  <E>C  style
definitions</E> (see Section&nbsp;<Ref Sect="Enumeration Style"/>). Making definitions  in
C style, that is filling the coset table line by line, it can be  very
advantageous to  switch  to  making  definitions  from  the  preferred
definition stack. Possible definitions  can  be  extracted  from  this
stack in  various  ways  and  the  two  options  <C>pmode</C>  and  <C>psize</C>
(see&nbsp;<Ref Subsect="option pmode"/> and&nbsp;<Ref Subsect="option psize"/>  respectively)  regulate  this.
However it should be clearly understood that  making  all  definitions
from a preferred definition stack one may  violate  the  condition  of
Mendelsohn's theorem, and the option <C>fill</C> (see&nbsp;<Ref Subsect="option fill"/>) can be
used to avoid this.
<Subsection Label="option fill">
<Heading><C>fill</C>, <C>ffactor</C></Heading>
<Index>option <C>fill</C></Index><Index>option <C>ffactor</C></Index>Controls the preferred definition strategy by setting the fill factor;
<C><A>val</A></C> must be a non-negative integer.
(Shortest abbreviation of <C>fill</C> is <C>fil</C>, and  shortest  abbreviation
of <C>ffactor</C> is <C>f</C>.)
<P/>
Unless prevented by the fill factor, gaps of length one  found  during
deduction  testing  are  preferentially   filled   (see <Cite Key="Hav91"/>).
However, this potentially violates the  formal  requirement  that  all
rows in the coset table are eventually filled (and tested against  the
relators). The fill factor  is  used  to  ensure  that  some  constant
proportion of the coset table is always kept filled. Before defining a
coset number to fill a  gap  of  length  one,  the  enumerator  checks
whether <C>fill</C> times the completed part of the table is at  least  the
total size of the table  and,  if  not,  fills  coset  table  rows  in
standard order (i.e.&nbsp;C style; see Section&nbsp;<Ref Sect="Enumeration Style"/>) instead
of filling gaps.
<P/>
An  argument of  0  selects  the default  value  of <M>\lfloor  5(n+2)/4
\rfloor</M>,  where <M>n</M>  is the  number of  columns in  the  table.  This
default  fill factor  allows  a moderate  amount  of gap-filling.   If
<C>fill</C> is  1, then there is  no gap-filling.  A large  value of <C>fill</C>
can cause  what is in effect  infinite looping (resolved  by the coset
enumeration failing).   However, in general,  a large value  does work
well.  The  effects of the various gap-filling  strategies vary widely.
It is  not clear  which values are  good general defaults  or, indeed,
whether any strategy is always <Q>not too bad</Q>.
<P/>
This option is identified as <C>Fi</C>  in  the  <Q>Run  Parameters</Q>  block
(obtained when <C>messages</C> is non-zero) of the &ACE; output, since for
the &ACE; binary, <K>fi</K> is an allowed abbreviation of <C>fill</C>. However,
<K>fi</K> is a &GAP; keyword and so the shortest  abbreviation  of  <C>fill</C>
allowed by the interface functions is <C>fil</C>.
</Subsection>
<Subsection Label="option pmode">
<Heading><C>pmode</C></Heading>
<Index>option <C>pmode</C></Index>Option for preferred definitions; <C><A>val</A></C> should  be  in  the  integer
range 0 to 3. (Shortest abbreviation: <C>pmod</C>.)
<P/>
The  value of  the  <C>pmode</C> option  determines  which definitions  are
preferred.  If  the argument is  0, then Felsch style  definitions are
made using  the next empty table  slot.  If the  argument is non-zero,
then gaps of length one found during relator scans in Felsch style are
preferentially  filled  (subject to  the  value  of  <C>fill</C>).  If  the
argument  is 1,  they are  filled  immediately, and  if it  is 2,  the
consequent deduction  is also made  immediately (of course,  these are
also put on the deduction stack).  If the argument is 3, then the gaps
of length one are noted in the preferred definition queue.
<P/>
Provided such a gap survives (and no coincidence occurs, which  causes
the queue to be discarded) the next coset number will  be  defined  to
fill the oldest gap of length one. The default value is either 0 or 3,
depending on the strategy selected (see Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>). If you want to know more details, read the code.
</Subsection>
<Subsection Label="option psize">
<Heading><C>psize</C></Heading>
<Index>option <C>psize</C></Index>Size of preferred definition queue; <C><A>val</A></C> <E>must</E> be 0 or <M>2^n</M>,  for
some integer <M>n&gt;0</M>. (Shortest abbreviation: <C>psiz</C>.)
<P/>
The  preferred definition  queue is  implemented as  a  ring, dropping
earliest entries. An argument of 0 selects  the default size of <M>256</M>.
Each  queue slot takes two words (i.e., 8 bytes),  and the  queue  can
store up to <M>2^n-1</M> entries.
</Subsection>
</Section>
<Section Label="ACE Parameter Options for R Style Definitions">
<Heading>ACE Parameter Options for R Style Definitions</Heading>
<Index>R style<Subkey>definition</Subkey></Index>
<Subsection Label="option row">
<Heading><C>row</C></Heading>
<Index>option <C>row</C></Index>Set the <Q>row filling</Q> option; <C><A>val</A></C> is either 0 or 1.
<P/>
By default, <Q>row filling</Q> is on (i.e.&nbsp;<K>true</K> or 1). To turn  it  off
set <C>row</C> to <K>false</K> or 0 (both are translated to 0 when passed to the
&ACE;  binary).  When   making   HLT   style   (i.e.&nbsp;R   style;   see
Section&nbsp;<Ref Sect="Enumeration Style"/>) definitions, rows of the coset table  are
scanned for holes after its coset  number  has  been  applied  to  all
relators, and definitions are made to fill any holes encountered. This
will,  in  particular,  guarantee  fulfilment  of  the  condition   of
Mendelsohn's  Theorem.  Failure  to  do  so  can  cause  even   simple
enumerations to overflow.
</Subsection>
<Subsection Label="option lookahead">
<Heading><C>lookahead</C></Heading>
<Index>option <C>lookahead</C></Index>Lookahead; <C><A>val</A></C> should be in the integer range 0 to 4.
(Shortest abbreviation: <C>look</C>.)
<P/>
Although HLT style strategies are fast, they are local, in  the  sense
that  the  implications  of  any  definitions/deductions  made   while
applying coset numbers may not become apparent until much  later.  One
way to alleviate this problem is to perform  lookaheads  occasionally;
that is, to test the information in the table, looking for  deductions
or  concidences.  &ACE;  can  perform  a  lookahead  when  the  table
overflows, before the compaction routine is called. Lookahead  can  be
done using the entire table or only that part of the table  above  the
current coset number, and it  can  be  done  R style  (scanning  coset
numbers from the  beginning  of  relators)  or  C style  (testing  all
definitions in all essentially different positions).
<P/>
The following are the effects of the possible values of <C>lookahead</C>:
<P/>
<List>
<Item>0 disables lookahead;</Item>
<Item>1 does a partial table lookahead, R style;</Item>
<Item>2 does a whole table lookahead, C style;</Item>
<Item>3 does a whole table lookahead, R style; and</Item>
<Item>4 does a partial table lookahead, C style.</Item>
</List>
<P/>
The default is 1 if the <C>hlt</C> strategy is used and  0  otherwise;  see
Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>.
<P/>
Section&nbsp;<Ref Sect="Enumeration Style"/> describes the various enumeration  styles,
and, in particular, R style and C style.
</Subsection>
</Section>
<Section Label="ACE Parameter Options for Deduction Handling">
<Heading>ACE Parameter Options for Deduction Handling</Heading>
<Subsection Label="option dmode">
<Heading><C>dmode</C></Heading>
<Index>option <C>dmode</C></Index>Deduction mode; <C><A>val</A></C> should be in the integer range 0 to 4.
(Shortest abbreviation: <C>dmod</C>.)
<P/>
A completed table  is only valid if every table  entry has been tested
in all essentially different  positions in all relators.  This testing
can either be done directly (<C>felsch</C> strategy;  see&nbsp;<Ref Subsect="option felsch"/>)
or via relator scanning (<C>hlt</C> strategy; see&nbsp;<Ref Subsect="option hlt"/>). If  it  is
done directly, then more than one deduction (i.e., table entry) can be
waiting to be processed at any one time. So  the  untested  deductions
are stored in a stack. Normally this stack is fairly small but, during
a collapse, it can become very large.
<P/>
This command allows the user  to  specify  how  deductions  should  be
handled. The value <A>val</A> has the following interpretations:
<P/>
<List>
<Item><M>0</M>:  
discard deductions if there is no stack space left;</Item>
<Item><M>1</M>: 
as for <M>0</M>, but purge any redundant coset numbers on the  top  of  the
stack at every coincidence;</Item>
<Item><M>2</M>: 
as for 0, but purge all redundant coset  numbers  from  the  stack  at
every coincidence;</Item>
<Item><M>3</M>:
discard the entire stack if it overflows; and</Item>
<Item><M>4</M>:
if the stack overflows, double the stack size and purge all  redundant
coset numbers from the stack.</Item>
</List>
<P/>
The default deduction mode is either <M>0</M>  or  <M>4</M>,  depending  on  the
strategy selected (see Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>), and it  is
recommended that you stay with the default. If you want to  know  more
details, read the well-commented C code.
<P/>
<E>Notes:</E>
If deductions are discarded for any reason, then a final relator check
phase  will be run  automatically at  the end  of the  enumeration, if
necessary, to check the result.
</Subsection>
<Subsection Label="option dsize">
<Heading><C>dsize</C></Heading>
<Index>option <C>dsize</C></Index>Deduction stack size; <C><A>val</A></C> should be a non-negative integer.
(Shortest abbreviation: <C>dsiz</C>.)
<P/>
Sets the  size of  the (initial) allocation  for the  deduction stack.
The size is  in terms of the number of  deductions, with one deduction
taking two words (i.e., 8 bytes).  The default size, of <M>1000</M>, can be
selected  by  a value  of  0.   See the  <C>dmode</C> entry  for a  (brief)
discussion of deduction handling.
</Subsection>
</Section>
<Section Label="Technical ACE Parameter Options">
<Heading>Technical ACE Parameter Options</Heading>
The following options do not affect how the coset enumeration is done,
but how it  uses the computer's resources. They  might thus affect the
runtime as  well as  the range of  problems that  can be tackled  on a
given machine.
<Subsection Label="option workspace">
<Heading><C>workspace</C></Heading>
<Index>option <C>workspace</C></Index>Workspace size in words (default <M>10^8</M>);
<C><A>val</A></C> should be an expression that evaluates to a positive  integer,
or a string of digits ending in a  <C>k</C>,  <C>M</C>  or  <C>G</C>  representing  a
multiplication  factor  of  <M>10^3</M>,  <M>10^6</M>  or  <M>10^9</M>,  respectively
e.g.&nbsp;both <C>workspace := 2 * 10^6</C> and <C>workspace :=  "2M"</C>  specify  a
workspace  of  <M>2\times10^6</M>  words.  Actually,  if   the   value   of
<C>workspace</C> is entered as a string, each of <C>k</C>, <C>M</C> or  <C>G</C>  will  be
accepted in either upper or lower case. (Shortest abbreviation: <C>wo</C>.)
<P/>
By default, &ACE; has a physical table size of <M>10^8</M> words (i.e., <M>4
\times 10^8</M> bytes in the  default 32-bit environment).  The number of
coset numbers in the table is  the table size divided by the number of
columns.   Although  the  number   of  coset  numbers  is  limited  to
<M>2^{31}-1</M> (if the C <C>int</C> type is 32 bits), the table size can exceed
the <M>4</M>GByte 32-bit limit if a suitable machine is used.
</Subsection>
<Subsection Label="option time">
<Heading><C>time</C></Heading>
<Index>option <C>time</C></Index>Maximum execution time in seconds; <C><A>val</A></C> must be an integer  greater
than or equal to <M>-1</M>. (Shortest abbreviation: <C>ti</C>.)
<P/>
The <C>time</C> command  puts a time limit (in seconds) on  the length of a
run. The default is <M>-1</M>  which is no  time limit. If the  argument is
<M>\ge0</M> then the total elapsed time for this call is checked at the end
of each pass through the enumerator's main loop, and if it's more than
the limit the run is stopped and the current table returned.
<P/>
Note that a limit of <M>0</M> performs exactly one pass  through  the  main
loop, since <M>0 \ge 0</M>.
<P/>
The time  limit is approximate, in  the sense that  the enumerator may
run for a longer, but never a shorter, time.  So, if there is, e.g., a
big collapse (so that the time round the loop becomes very long), then
the run may run over the limit by a large amount.
<P/>
<E>Notes:</E>
The time limit is CPU-time, not wall-time.  As  in  all  timing  under
UNIX, the clock's granularity  (usually  <M>10</M>  milliseconds)  and  the
system load can  affect  the  timing;  so  the  number  of  main  loop
iterations in a given time may vary.
</Subsection>
<Subsection Label="option loop">
<Heading><C>loop</C></Heading>
<Index>option <C>loop</C></Index>Loop limit; <C><A>val</A></C> should be a non-negative integer.
<P/>
The core enumerator is organised as a state machine,  with  each  step
performing an <Q>action</Q> (i.e., lookahead, compaction) or a  block  of
actions (i.e.,  <M>|`ct'|</M>  coset  number  definitions,  <M>|`rt'|</M>  coset
number applications). The number  of  passes  through  the  main  loop
(i.e., steps) is counted, and the enumerator can make an early  return
when this count hits the value of <C>loop</C>. A value of <M>0</M>, the default,
turns this feature off.
<P/>
<E>Guru Notes:</E>
You can do lots of really neat things using this feature, but you need
some understanding of the internals of &ACE; to get real benefit from
it.
</Subsection>
<Subsection Label="option path">
<Heading><C>path</C></Heading>
<Index>option <C>path</C></Index>Turns on path compression.
<P/>
To correctly  process  multiple  concidences,  a  union-find  must  be
performed. If both path compression and weighted union are used,  then
this can be done in essentially linear time (see, e.g., <Cite Key="CLR90"/>).
Weighted union alone, in the worst-case, is worse than linear, but  is
subquadratic. In practice, path compression  is  expensive,  since  it
involves many coset table accesses. So, by default,  path  compression
is turned off; it can be turned on by <C>path</C>. It has no effect on  the
result, but may affect the running time and the internal statistics.
<P/>
<E>Guru Notes:</E>
The whole question of the best way to handle large coincidence forests
is problematic.  Formally, &ACE; does  not do a weighted union, since
it is constrained to replace the higher-numbered of a coincident pair.
In practice,  this seems  to amount to  much the same  thing!  Turning
path  compression on  cuts down  the  amount of  data movement  during
coincidence processing at the expense of having to trace the paths and
compress them.  In general, it does not seem to be worthwhile.
</Subsection>
<Subsection Label="option compaction">
<Heading><C>compaction</C></Heading>
<Index>dead coset (number)</Index><Index>option <C>compaction</C></Index>Percentage of dead coset numbers to trigger compaction; 
<C><A>val</A></C> should be an integer (percentage) in the integer  range  0  to
100. (Shortest abbreviation: <C>com</C>.)
<P/>
<Index>dead coset (number)</Index>
The option <C>compaction</C> sets the percentage of  <E>dead</E>  coset  numbers
needed  to  trigger  compaction  of  the  coset   table,   during   an
enumeration. A <E>dead</E> coset (number) is a coset  number  found  to  be
coincident with a smaller coset number. The  default  is  10  or  100,
depending on the strategy  used  (see  Chapter&nbsp;<Ref Chap="Strategy Options for ACE"/>).
<P/>
Compaction recovers the space allocated to  coset  numbers  which  are
flagged as dead. It results in a table  where  all  the  active  coset
numbers are numbered contiguously from 1, and with  the  remainder  of
the table available for new coset numbers.
<P/>
The coset table is compacted when a definition of a  coset  number  is
required, there is no space for a  new  coset  number  available,  and
provided that the given percentage of the coset  table  contains  dead
coset numbers. For example, if <C>compaction</C>  =  <M>20</M>  then  compaction
will occur only if 20% or more of the coset numbers in the table  are
dead. An argument of 100 means that  compaction  is  never  performed,
while an argument of 0 means always compact, no matter  how  few  dead
coset numbers there are (provided there is at least one, of course).
<P/>
Compaction may be performed  multiple times during an enumeration, and
the table that results from an  enumeration may or may not be compact,
depending on whether or not there have been any coincidences since the
last compaction (or  from the start of the  enumeration, if there have
been no compactions).
<P/>
<E>Notes:</E>
In some strategies (e.g., <C>hlt</C>; see&nbsp;<Ref Subsect="option hlt"/>) a  lookahead  phase
may be run before compaction is attempted. In other strategies  (e.g.,
<C>sims := 3</C>; see&nbsp;<Ref Subsect="option sims"/>)  compaction  may  be  performed  while
there are  outstanding  deductions;  since  deductions  are  discarded
during compaction, a final lookahead  phase  will  (automatically)  be
performed.
<P/>
<Index>dead coset (number)</Index>
Compacting a table <Q>destroys</Q> information and history, in the  sense
that the coincidence list is deleted, and the table  entries  for  any
dead coset numbers are deleted.
</Subsection>
<Subsection Label="option max">
<Heading><C>max</C></Heading>
<Index>option <C>max</C></Index>Sets the maximum coset number that can be defined;
<C><A>val</A></C> should be <M>0</M> or an integer greater than or equal to 2.
<P/>
By default (which is the case <C>max</C><M>{}=0</M>), all of  the  workspace  is
used, if necessary, in building the coset table. So the table size (in
terms of the number of rows) is an  upper  bound  on  how  many  coset
numbers can be alive at any one time. The <C>max</C> option allows a  limit
to be placed on how much physical table space is made available to the
enumerator. Enough space for at least two  coset  numbers  (i.e.,  the
subgroup and one other) must be made available.
<P/>
<E>Notes:</E>
If the <C>easy</C>  strategy  (see&nbsp;<Ref Subsect="option easy"/>)  is  selected,  so  that
<C>compaction</C> (see&nbsp;<Ref Subsect="option compaction"/>) is off (i.e.&nbsp;set  to  100)  and
<C>lookahead</C> (see&nbsp;<Ref Subsect="option lookahead"/>) is off (i.e.&nbsp;set to 0), and <C>max</C>
is set to a positive integer, then coset numbers are not  reused,  and
hence <C>max</C> bounds the <E>total</E> number <C>totcosets</C> (see  Section&nbsp;<Ref Sect="Coset Statistics Terminology"/>)  of  coset   numbers   defined   during   an
enumeration.
<P/>
On the other hand, if one (or both) of <C>compaction</C> or <C>lookahead</C>  is
not off, then some reuse of coset numbers may occur, so that, for  the
case where <C>max</C> is a positive integer, the value of  <C>totcosets</C>  may
be greater than <C>max</C>.
<P/>
However,  whenever  <C>max</C>  is  set  to  a   positive   integer,   both
<E>activecosets</E> (the number of <E>alive</E> coset numbers at the end  of  an
enumeration) and  <E>maxcosets</E>  (the  maximum  number  of  alive  coset
numbers at any point of an enumeration)  are  bounded  by  <C>max</C>.  See
Section&nbsp;<Ref Sect="Coset Statistics Terminology"/>,  for  a  discussion  of  the
terminology: <E>activecosets</E> and <E>maxcosets</E>.
</Subsection>
<Subsection Label="option hole">
<Heading><C>hole</C></Heading>
<Index>option <C>hole</C></Index>Maximum percentage of holes allowed during an enumeration;
<C><A>val</A></C> should be an integer in the range <M>-1</M> to 100.
(Shortest abbreviation: <C>ho</C>.)
<P/>
This is an experimental feature which  allows  an  enumeration  to  be
terminated when the percentage of holes in the table exceeds  a  given
value. In practice, calculating this is very expensive, and  it  tends
to remain constant or  decrease  throughout  an  enumeration.  So  the
feature doesn't seem very useful. The default value of <M>-1</M> turns this
feature off. If you want more details, read the source code.
</Subsection>
</Section>
<Section Label="ACE Parameter Options controlling ACE Output">
<Heading>ACE Parameter Options controlling ACE Output</Heading>
<Subsection Label="option messages">
<Heading><C>messages</C>, <C>monitor</C></Heading>
<Index>option <C>messages</C></Index><Index>option <C>monitor</C></Index>Sets the verbosity of output from &ACE;; <A>val</A> should be an integer.
(Shortest  abbreviation  of  <C>messages</C>  is   <C>mess</C>,   and   shortest
abbreviation of <C>monitor</C> is <C>mon</C>.)
<P/>
By default, <A>val</A> = 0, for which &ACE; prints out only a single  line
of information, giving the result of each  enumeration.  If  <A>val</A>  is
non-zero then the presentation and the parameters are  echoed  at  the
start of the run, and messages  on  the  enumeration's  status  as  it
progresses are also printed out. The absolute value of <A>val</A> sets  the
frequency of the progress messages, with a negative sign turning  hole
monitoring on. Note that, hole monitoring is expensive, so don't  turn
it on unless you really need it.
<P/>
Note   that,  ordinarily,  one   will   not   see   these    messages:
non-interactively,   these   messages    are    directed    to    file
<C>ACEData.outfile</C>   (or   <A>filename</A>,   if   option   <C>aceoutfile   :=
<A>filename</A></C>, or <C>ao := <A>filename</A></C>, is used), and interactively  these
messages are simply  not  displayed.  However,  one  can  change  this
situation both interactively and   non-interactively  by  setting  the
<C>InfoLevel</C> of <C>InfoACE</C> to 3 via
<P/>
<Example><![CDATA[
gap> SetInfoLevel(InfoACE, 3);
]]></Example>
<P/>
Then &ACE;'s messages are  displayed  prepended  with  <Q>&#x2018;#I  </Q>'.
Please refer to Appendix&nbsp;<Ref Chap="The Meanings of ACE's output messages"/>,
where the meanings of &ACE;'s output messages are fully discussed.
</Subsection>
</Section>
<Section Label="ACE Parameter Options that give Names to the Group and Subgroup">
<Heading>ACE Parameter Options that give Names to the Group and Subgroup</Heading>
These options may be safely ignored; they only give names to the group
or subgroup within the &ACE;  output,  and  have  no  effect  on  the
enumeration itself.
<Subsection Label="option enumeration">
<Heading><C>enumeration</C></Heading>
<Index>option <C>enumeration</C></Index>Sets the <C>Group Name</C> to <A>string</A>;  <A>string</A>,  must  of  course  be  a
string. (Shortest abbreviation: <C>enum</C>.)
<P/>
The &ACE; binary has a two-word synonym for this option: <C>Group Name</C>
and this is how it is identified in the <Q>Run  Parameters</Q>  block  of
the &ACE; output when <C>messages</C> has a non-zero  value.  The  default
<C>Group Name</C> is <C>"G"</C>.
</Subsection>
<Subsection Label="option subgroup">
<Heading><C>subgroup</C></Heading>
<Index>option <C>subgroup</C></Index>Sets the <C>Subgroup Name</C> to <A>string</A>; <A>string</A> must  of  course  be  a
string. (Shortest abbreviation: <C>subg</C>.)
<P/>
The default <C>Subgroup Name</C> is <C>"H"</C>.
</Subsection>
</Section>
<Section Label="Options for redirection of ACE Output">
<Heading>Options for redirection of ACE Output</Heading>
<Subsection Label="option ao">
<Heading><C>ao</C>, <C>aceoutfile</C></Heading>
<Index>option <C>ao</C></Index><Index>option <C>aceoutfile</C><Subkey>ao synonym</Subkey></Index>Redirects (<C>a</C>lters) <C>o</C>utput to <A>filename</A>; <A>filename</A>  should  be  a
string.
<P/>
Non-interactively, output from &ACE; is normally directed to a temporary
file whose full path is stored in <C>ACEData.outfile</C>, which  is  parsed
to produce a coset table or a list of statistics. This  option  causes
&ACE;'s output to  be  directed  to  <A>filename</A>  instead,  presumably
because the user wishes to see (and keep) data output  by  the  &ACE;
binary,    other    than    the    coset     table     output     from
<C>ACECosetTableFromGensAndRels</C> or the statistics output by <C>ACEStats</C>.
Please refer to Appendix&nbsp;<Ref Chap="The Meanings of ACE's output messages"/>,
where we discuss the meaning of the additional data to be found in the
&ACE; binary's output. The option <C>aceoutfile</C> is a &GAP;-introduced
synonym for <C>ao</C>, that is translated to <C>ao</C> before submission to  the
&ACE; binary.  Do  not  use  option  <C>aceoutfile</C>  when  running  the
standalone directly. Happily, <C>ao</C> can also be regarded as  mnemonical
for <C>aceoutfile</C>.
</Subsection>
</Section>
<Section Label="Other Options">
<Heading>Other Options</Heading>
&ACE; has a number of other options, but the  &GAP;  user  will  not
ordinarily need them, since, in most  cases,  alternative  interactive
functions exist.  These  remaining  options  have  been  relegated  to
Appendix&nbsp;<Ref Chap="Other ACE Options"/>. The options listed  there  may  be  used
both interactively and non-interactively, but many are  probably  best
used directly via the &ACE; standalone.
</Section>
</Chapter>
